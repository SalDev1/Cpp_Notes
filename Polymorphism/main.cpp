#include <iostream>
using namespace std;

int main()
{
    // It will use a base pointer and manage derives class objects.

    /*
      Polymorphism is about using base class pointers or references to manage derived
      class objects in our inheritance hierarchies.

      It can be managing multiple kinds of objects whose class
      are part of our inhertiance is called polymorphism.

      Polymorphism:-
      Managing derived objects in memory though base pointers or references and getting the
      right method called on the base pointer or reference.
    */
    return 0;
}